# Frontend environment variables
# Use `process.env.REACT_APP_API_URL` in the React app
REACT_APP_API_URL=http://localhost:5000
# Optional: external receiver (webhook or remote API) to forward created user data to
# Example: REACT_APP_EXTERNAL_API_URL=https://remote.example.com/receive
REACT_APP_EXTERNAL_API_URL=
